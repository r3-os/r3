[package]
name = "r3_kernel"
version = "0.0.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "An RTOS kernel implementing the R3 API (placeholder)"
categories = ["embedded", "no-std"]
keywords = ["r3"]
repository = "https://github.com/r3-os/r3"

[dependencies]
